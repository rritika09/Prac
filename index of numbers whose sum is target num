# Online Python compiler (interpreter) to run Python online.
# Write Python 3 code in this online editor and run it.
print("Hello world")
num = [2,3,7,9,1]
target = 9
tgt_index=[]
for i in range(len(num)):
    diff = target-num[i]
    if diff in num:
        tgt_index.append(num[i])
        tgt_index.append(diff)
        j=num.index(diff)
        print(j)
        break
print(tgt_index)
